package olcproyecto1;
//import Estructuras.Nodo;
import Analisis.*;


public class OLCProyecto1 {
 /*   public static int contador;
    public static String codigo;
    

    
    public static String imprimirArbol(Nodo raiz){
        codigo = "digraph G{\n";
        if(raiz.lexema.isEmpty())
            codigo += "n0[label=\"" + raiz.token + "\"];\n";
        else
            codigo += "n0[label=\"" + raiz.lexema + "\"];\n";
        contador = 1;
        recorrerArbol("n0",raiz);
        codigo +="}";
        return codigo;
    }
    public static void recorrerArbol(String raiz, Nodo padre){
        for(Nodo hijo:padre.hijos){
            String nombre_hijo = "n"+contador;
            if(hijo.lexema.isEmpty()){
                codigo += nombre_hijo + "[label=\"" + hijo.token.replace("\"","&#34;") + "\"];\n";//replace es temporal
            }
            else
                codigo += nombre_hijo + "[label=\"" + hijo.lexema + "\"];\n";
            codigo += raiz + "->" + nombre_hijo + ";\n";
            contador++;
            recorrerArbol(nombre_hijo, hijo);
        }
    }*/
    
    
    public static void main(String[] args) {
        /*Analisis principal = new Analisis();
        principal.analizar("D:\\Sistemas\\2018\\OLC\\practica_1\\", "D:\\Sistemas\\2018\\OLC\\proyecto_1\\");
        if(principal.getRaiz()!=null){
            System.out.println("Analisis completo");
            principal.generarJSON();
        }else{
            System.out.println("Error al analizar");
        }*/
        

    }
    
}
